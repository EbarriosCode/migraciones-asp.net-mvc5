@model IEnumerable<relaciones.Models.Viaje>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Cliente.Nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Vendio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cobro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @item.Cliente.Nombre
        </td>
        <td>
            @item.Vendio
        </td>
        <td>
            @item.Cobro
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdViaje }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdViaje }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdViaje })
        </td>
    </tr>
}

</table>
